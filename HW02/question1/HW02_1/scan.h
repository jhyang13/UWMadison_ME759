#ifndef SCAN_H
#define SCAN_H

#include <cstddef>

// Performs an inclusive scan on input array arr and stores n 
// Advised by using void scan(const float *arr, float *output, std::size_t n);
void scan(const float *arr, int n);

#endif
